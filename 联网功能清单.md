# 离线私服：做不了 / 需要联网的功能清单

生成时间：本轮；对应 mock 版本 v0.46.0

## 一、必须真实联网（已跳过，客户端里保持“失败关闭”/不触发）
| 功能 | 客户端入口 | 为什么做不了 | 现状 |
|---|---|---|---|
| 真实支付/充值 | RechargeViewController → 渠道支付 | 需要渠道 SDK 与真实订单 | 已改为“内购免费”：`new/iap.js` 直接发货，不再走支付 |
| 激励视频广告 | AdsModel / `adsmgr_*` | 需要广告平台 SDK | mock 一律 `code:0` + 无广告位，客户端自动走非广告分支 |
| 微信/QQ 登录、分享 | EjoySDK / ShareView | 需要第三方 App 与开放平台 | Test 渠道直登；分享按钮点了只走本地回调 |
| 排行榜 | `rank_load` / `rank_like` | 榜单是全服数据 | 已回本地假榜（自己一个人）；真实排名不可能还原 |
| 好友/串门/拜访 | `guest_*` / `visit_*` | 需要其他玩家 | 用本地访客模拟（visitor.js） |
| 淘宝版物品导入 | `client_draw_taobao` | 需要淘宝授权数据 | 回 `{ok:false}`，永不弹导入窗 |
| 微信小游戏推送 | `wx_push_*` | 需要微信后台 | push_switch 关，相关入口不显示 |
| 实名认证/防沉迷 | EjoySDK 实名 | 需要公安接口 | 直接判定已实名 |
| 云存档 | 服务器存档 | 需要服务端 | 改为本地 `/save` + `/load`（JSON），重启不丢 |
| 激活码/兑换码 | `mail_ejoy_active_code` | 需要官方码池 | 回 `{code:0}`，不发奖 |
| 公告/GM 后台 | `client_gm` / 公告 | 需要运营后台 | 关闭 |
| 资源 CDN 更新 | `version.json` 比对 | 停服后 CDN 已不可用 | 全部走本地资源（assets 内已含全部美术/音频） |

## 二、可以完全离线还原（已实现）
存档/状态机/出发/归来结算/相册/背包/商店/三叶草与四叶草/邮件/旅友投喂/故事与羁绊/抽奖/手工与家具/聚会/限时活动/图鉴。

## 三、半离线（本地近似，行为一致但非原服数据）
- 明信片构图：用客户端自带的 `Picture_json` + `resources_json` 现场合成（原服是服务端下发图层，算法无法 100% 复原，版式已对齐）。
- 限时活动时间：按真实日历（春卡=春节窗口、贺卡=12/20~1/10、博物馆日=5/10~5/31），原服的运营档期无从得知。
- 旅行时长/目的地权重：原服按食物+道具+权重计算，我们按同一思路本地实现（180~420 秒可调）。