# lxqw 离线模拟服 —— 做不了的功能清单（截至本轮）

> 判定口径：**客户端能做的都做了**（真机可玩）；下面这些要么必须联网/第三方服务，要么客户端本身没有对应协议或数据。
> 证据都在 `NOTES.md` 对应章节与两个只读审计报告里（`apk/assets/game/js/main.min.js` 的字节偏移可复核）。

## 一、必须联网 / 第三方服务（10 类，离线无法实现）

| # | 功能 | 客户端入口 | 为什么做不了 |
|---|---|---|---|
| 1 | **微信/QQ 分享与分享奖励** | `share_load` / `share_get_reward` / `adsmgr_share` / `client_share_publicity` | 走 `wx.*` 小游戏能力 + 开放平台回调验签；分享图还硬编码了 `mmocgame.qpic.cn/wechatgame/...` |
| 2 | **激励视频 / 插屏广告** | `adsmgr_load` / `adsmgr_shop_free` / `get_ad_info`、`item_buy.ads_id`、`mail.resource.ads_id`、料理"看广告" | 需广告平台 SDK 与服务端回调校验；离线只能回 `{code:0}` 让客户端走非广告分支 |
| 3 | **内购 / 充值** | `recharge_*` + 原生支付（ALISDK/EjoySDK） | 需要商店签名与支付回调；现在由 `new/iap.js` 直接发货（免费内购） |
| 4 | **账号登录 / 实名防沉迷** | `hall_login` | 走第三方账号体系与实名接口；离线用 `offline-token` 绕开（客户端内置的 `anti_addiction_overtime.html`、`appeal.lingxigames.com` 页面同样需要网络） |
| 5 | **排行榜** | `rank_load` / `rank_like` / `rank_get_intro` | 榜单是全服数据；现在是本地单人假榜 |
| 6 | **公告 / 推送** | 渠道 SDK `getAnnInfo({type:"maintain"/"common"})`、`client_switch_push`、`wx_*` 订阅消息 | 渠道 HTTP 接口 + 微信后台；离线拿不到（**墙上的地图**例外，见下） |
| 7 | **淘宝导入** | `client_draw_taobao` / `client_taobao_import` | 需淘宝开放平台授权；只能回 `{ok:false}` |
| 8 | **官方兑换码池** | `item_use_gift_code` | 官方码池随停服消失；现改成"任意非空码发一次奖"（成功回 `code:200`，与客户端 UI 一致） |
| 9 | **协议签名 / 加密** | `core.SocketManage.send` 全链路 | 客户端走 WebSocket + md5/encrypt；模拟服直接改写 `send`/`Socket` 绕开整条链路，因此任何"与官方服务器对话"的功能（真榜、活动开奖、跨端数据）都做不了 |
| 10 | **远程资源类图片** | `RES.getResByUrl(...)` | 微信头像/分享图等远程图；本地美术已随 APK 托管，不受影响 |

### 唯一被"半离线"救回来的联网功能：墙上的地图
`MainInView.on_enterTravelMap_tap` 要求 `ActivityModel.getActivity("travelmap").params.isOpen`，
这个活动来自渠道公告。现在 `new/travelmap.js` 把当前渠道单例的 `getAnnInfo` 包一层，
只劫持 `tags:["travelmap"]` 那一次调用，返回本机 `http://127.0.0.1:8089/travelmap.html`
（页面自己 `GET /load` 读存档，渲染三叶草/明信片/日记/故事/家具/物品/旅行次数 + 去过的地方）；
其它公告（维护/防沉迷）原样透传。**注意**：真正把网页弹出来要靠原生 WebView，若原生层不响应，
行为与以前（点了没反应）一致，不会有副作用。

## 二、客户端本身不支持（做不了，不是我们没做）

| # | 清单条目 | 事实 |
|---|---|---|
| 1 | 旅友"**猫咪、蚂蚁、蜗牛、蜜蜂、乌龟**" | `tables/TravelFriends_json.json` 只有 3 位：壁虎(0)/刺猬(1)/萤火虫(2)；客户端 China 版固定 `["wugui","maotouying3","songshu"]`。表里没有的角色无法凭空补 |
| 2 | 图鉴"**道具 / 旅友收集进度**" | 客户端 `ItemModel.item_load_handbook` 只读 `collections` / `specialtys` 两个 id 数组，没有"道具/旅友进度"的协议口子；图鉴本身（`encyclopedia_load`/`encyclravel_load`）已用 `config.eab` 的真数据实现 |
| 3 | 道具"**使用效果**"（加速/耐久） | 客户端 245 条协议里**没有** `item_use*`；"使用"= 放进背包/桌面 + 出发时消耗。工具耐久/加速没有字段可写 |
| 4 | 明信片"**文案 + 时间**" | photo 对象客户端只读 `{id, pic_id, layers}`，没有文案/时间字段 |
| 5 | 商店"**服务端轮换商品**" | 客户端商店列表来自它自己的 `ShopDataDB`（静态表），服务端唯一杠杆是 `purchased`（限购/`before_buy` 解锁链，已实现）与嘟嘟的 `num`（已实现） |
| 6 | 活动"**限定明信片**" | 客户端明信片池没有"活动限定"分类（只有 Normal/Tools/Goal/Unique），无字段可挂 |

## 三、刻意不做（设计选择）

| # | 清单条目 | 决定与理由 |
|---|---|---|
| 1 | 离线结算"**多趟累计补算**" | 原版是"出门一次、回来要玩家再送出门"；自动补算多趟等于凭空发奖励，也与日记/明信片一条条来的叙事不符。当前实现：启动后 4 秒内结算**已归来的那一趟** |
| 2 | 三叶草/四叶草"**权重表**" | 客户端契约里只有 `element 0/1/2`（+1 三叶草 / 四叶草 / sprite）三档，概率由服务端决定；当前用固定 12%/10%（`rules.js` 的 `cloverRoll`），改权重不影响客户端 |
| 3 | 旅友口味"**偏好表**" | 口味由**客户端** `CharaDB.taste` + `SpecialtyDB` 自己算（`getFriendTaste`），服务端只按口味分给回礼；服务端再存一份偏好表没有意义 |

## 四、仍然可做但优先级低（如果以后还想继续）
1. `lottery_open` 的"开局物品"目前固定四叶草×1，可改成按权重抽（周末抽奖的奖池来自客户端 `lotteryData`）；
2. 聚会结果里的"绘纸 gift"（`page`/`coll` 两个字段现在是 0）可以接上 `drawing.js` 的收藏；
3. 手工/家具的制作队列（目前单槽，一件完再开一件）；
4. 访客/邻居的作息表（现在是随机定时）。
